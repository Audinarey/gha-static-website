name: Env Demo
on: workflow_dispatch
env: 
  APP_ENV: "dev"
jobs:
  env_demo:
    env: 
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Connection to env
        run: echo "App is connection to $APP_ENV"
